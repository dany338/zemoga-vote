import React from 'react'
import { Container, Search, Footer } from './header.styles';
export interface ICardProps {}

const Header: React.FC<ICardProps> = ({ }) => {
  return (
    <div>
        
    </div>
  )
}

export default Header;
